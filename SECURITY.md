# Security Policy

Security fixes are applied to the latest commit on the default branch and the
latest published container image.

The mounted Docker socket is root-equivalent control of the host. Report any
vulnerability through GitHub private vulnerability reporting, not a public
issue. Include reproduction steps and impact without including production
socket endpoints or credentials. You can expect acknowledgement within seven
days.
